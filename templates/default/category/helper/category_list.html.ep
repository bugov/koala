<select name="category_id">
% for my $c (@$list) {
  <option
    <% if ($c->id == $default) { %> selected="selected" <% } %>
    value="<%= $c->id %>"
  ><%= $c->title %> [<%= $c->url %>]</option>
% }
</select>